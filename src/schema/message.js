const message = {
  emailRegistred: 'Email already registered',
  invalidEntries: 'Invalid entries. Try again.',
  unfilledFields: 'All fields must be filled',
  wrongEmailPassword: 'Incorrect username or password',
};

module.exports = { message };
